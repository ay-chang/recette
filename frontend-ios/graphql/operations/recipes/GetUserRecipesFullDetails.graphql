query GetUserRecipesFullDetails($email: String!) {
  userRecipes(email: $email) {
    id
    title
    description
    steps
    imageurl
    tags {
      name
    }
    difficulty
    servingSize
    cookTimeInMinutes
  }
}
