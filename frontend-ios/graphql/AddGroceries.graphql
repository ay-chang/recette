mutation addGroceries($groceries: [GroceryInput!]!, $email: String!, $recipeId: String!) {
  addGroceries(groceries: $groceries, email: $email, recipeId: $recipeId) {
    id
    name
    measurement
  }
}
